To-Do:

-Set up sql schema DONE
-Run on mysql local DONE
-Create Java code to work with specified schema in local mysql repository MEH(ish)
-Create friend recommendations algo, create search algo (can be done kinda easily through sql) (Half Done)
-Learn how to use GWT, Apache Tomcat servlets

Notes:
-Create html form for user sign up at Pennbook index.html page to push to Apache Tomcat servlet.

username is the email because we are lazy. 

Do we want friendships to include Strenght & Type or not??

Need to Finish Testing:
getInterests, getFriends, getWallposts, uSearch, tSearch, iSearch, userCheck, addUser, postMsg, addFriend